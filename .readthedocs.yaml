version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands:
    - pip install poetry
    - poetry install --with docs
    - poetry run sphinx-build -b html docs $READTHEDOCS_OUTPUT/html
